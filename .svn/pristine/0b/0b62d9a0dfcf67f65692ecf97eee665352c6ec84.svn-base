#include "2310A.h"



int main(int argc, char **argv) {

    // TODO temp argv. Put into separate func
    FILE *loadedFile = fopen(argv[1], "r");

    Path path;
    alloc_path(&path, loadedFile, 2);

    printPath(&path);

    Player playerA;
    Player playerB;

    initPlayer(&playerA, 0);
    initPlayer(&playerB, 1);

    movePlayer(&playerA, 5);


}
